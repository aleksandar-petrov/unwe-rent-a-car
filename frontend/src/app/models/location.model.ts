export interface LocationRequest {
  latitude: number;
  longitude: number;
  address: string;
  city: string;
  country: string;
}

export interface LocationResponse {
  latitude: number;
  longitude: number;
  address: string;
  city: string;
  country: string;
}
